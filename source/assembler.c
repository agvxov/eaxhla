#include "assembler.h"

static void assemble_xor (size_index size,
                          type_index type,
                          size_t     destination,
                          size_t     source) {
	// ERROR CHECKING

	// byte_array [byte_count] = ...;

	// byte_count += ...;
}
